name = "sazanami-web"
compatibility_date = "2025-05-12"

# Environment variables (non-secret - for dev)
[vars]
STORAGE_DRIVER = "local"

# Node.js compatibility
compatibility_flags = ["nodejs_compat"]

# Externalize non-Workers-compatible packages
[build]
external = ["nodemailer"]
